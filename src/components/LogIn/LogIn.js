import Header from '../Header/Header';
import Navigation from '../Navigation/Navigation';

const LogIn = () => (
  <>
    <Header />
    <Navigation />
    <div>Login</div>
  </>
);

export default LogIn;
