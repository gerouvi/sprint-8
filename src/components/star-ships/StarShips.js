import Header from '../header/Header';
import ListShips from '../list-ships/ListShips';
import Navigation from '../navigation/Navigation';

const StarShips = () => {
  return (
    <div>
      <Header />
      <Navigation />
      <ListShips />
    </div>
  );
};

export default StarShips;
